{
  "name": "agro-project-strapi",
  "private": true,
  "scripts": {
    "build": "cd backend && npm run build",
    "start": "cd backend && npm run start",
    "develop": "cd backend && npm run dev",
    "install": "cd backend && npm install"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
