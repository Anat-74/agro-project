name: Full Deploy

on:
  push:
    paths-ignore:
      - 'README.md'
      - 'LICENSE'
      - '.gitignore'
  workflow_dispatch:

jobs:
  frontend-deploy:
    runs-on: ubuntu-latest
    if: contains(github.event.head_commit.modified, 'frontend')
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: 'frontend/package-lock.json'
    
    - name: Install frontend dependencies
      run: npm ci
      working-directory: frontend
    
    - name: Build frontend
      run: npm run build
      working-directory: frontend
      env:
        NODE_ENV: production
    
    - name: Deploy frontend
      run: |
        echo "Deploying frontend..."
        # Добавьте сюда команды для деплоя вашего Nuxt.js приложения

  backend-deploy:
    runs-on: ubuntu-latest
    if: contains(github.event.head_commit.modified, 'backend')
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: 'backend/package-lock.json'
    
    - name: Install backend dependencies
      run: npm ci
      working-directory: backend
    
    - name: Build backend
      run: npm run build
      working-directory: backend
    
    - name: Deploy backend
      run: |
        echo "Deploying backend..."
        # Добавьте сюда команды для деплоя вашего Strapi приложения