{
  "collectionName": "components_calc_defaults",
  "info": {
    "displayName": "Calculator defaults",
    "description": "Default units and typical parameters for the calculator"
  },
  "options": {},
  "attributes": {
    "units": {
      "type": "enumeration",
      "enum": [
        "sqm",
        "are"
      ],
      "default": "sqm",
      "description": "Area units: sqm (m²) or are (100 m²)"
    },
    "defaultArea": {
      "type": "decimal",
      "description": "Default area"
    },
    "defaultRowSpacing": {
      "type": "decimal",
      "description": "Default row spacing, cm"
    },
    "defaultPlantSpacing": {
      "type": "decimal",
      "description": "Default in-row spacing, cm"
    }
  }
}
