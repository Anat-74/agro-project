# План работ — Agro Market

## 🚫 Режим удалённой работы

> Если диалог идёт **удалённо** (не за компьютером) — **никаких установок и обновлений**.
> `npm install`, `nuxi module add`, `nuxt upgrade` и любые другие долгие операции — **только когда пользователь за ПК**.
> Причина: длинные/многословные команды сбрасывают snapshot и прерывают диалог.

## ⚠️ Ключевое правило работы

> **НИКАКИХ ДЕЙСТВИЙ без прямого утверждения пользователем.**
> Перед каждым шагом — получить одобрение.
> После каждого действия — обязательный отчёт/обратная связь.
> **Никаких `git push` без явного разрешения пользователя.**

---

## Задачи

### 1. ✅ Исправить конфигурацию `nuxt-spyglass-mcp` в `kilo.json`

**Проблема:** В `kilo.json` используется `npx.cmd -y nuxt-spyglass-mcp`, но `nuxt-spyglass-mcp` — не отдельный npm-пакет, а бинарник внутри пакета `nuxt-spyglass`. Флаг `-y` вызывает 404.

**Сделано:**
- Команда заменена на прямой запуск через `node` с абсолютным путём к `mcp.mjs`
- Причина: npx не видит бинарник, т.к. `node_modules` в `frontend/`, а Kilo запускает MCP из корня проекта

**Файл:** `C:\agro-project\.kilo\kilo.json`

---

### 2. ✅ Установить и настроить `@nuxt/fonts`

**Что даёт:**
- Автоматическая оптимизация метрик шрифтов → устранение CLS (Core Web Vitals)
- Zero-config — удаляем ручные `@font-face` и preload
- Кэширование шрифтов через `unstorage`

**Сделано:**
- Установлен `@nuxt/fonts@0.14.0`, добавлен в `nuxt.config.ts`
- Удалён `_fonts.scss` (7 ручных `@font-face`)
- Удалён импорт `_fonts.scss` из `styles.scss`
- Удалены 7 preload-ссылок из `app.vue` (оставлен preconnect для Strapi)
- Переименованы `OpenSans-*` → `Open-Sans-*` для корректного matching
- Добавлена конфигурация шрифтов (`provider: local` для 3 семейств)

---

### 3. ✅ Установить и настроить `@nuxt/eslint`

**Что даёт:**
- Project-aware линтинг (JS, TS, Vue) с flat config (ESLint v9)
- Автогенерация `eslint.config.mjs` под структуру Nuxt 4
- Проверка кода на этапе сборки / через `npm run lint`

**Сделано:**
- Установлен `@nuxt/eslint@1.16.0` + `eslint@10.6.0`
- Создан `eslint.config.mjs` (корень проекта) и `.nuxt/eslint.config.mjs` (авто)
- Модуль добавлен в `nuxt.config.ts`
- Добавлены скрипты `lint` / `lint:fix` в `package.json`

**Что НЕ заменяет:**
- Расширение VS Code (`dbaeumer.vscode-eslint`) — нужно и дальше, оно даёт подсветку в редакторе в реальном времени

---

### 4. ✅ Установить `@nuxt/hints`

**Статус:** ожидает утверждения

**Что даёт:**
- Визуальный Web Vitals мониторинг (LCP, CLS, INP) в Nuxt DevTools с привязкой к элементам
- Hydration Mismatch Inspector (side-by-side diff SSR vs Client)
- HTML Validate (`html-validate`) — проверка валидности HTML в dev-режиме
- Lazy Load Detection — находит компоненты, которые можно перевести в ленивую загрузку
- Third-Party Scripts аудит (безопасность, производительность)
- Actionable console-советы ("LCP Element should not have loading='lazy'")

**Что нужно сделать:**
1. `npx nuxt module add hints`
2. Удалить `composables/useWebVitals.ts` (полностью)
3. Убрать из `app.vue` строки импорта и вызова `useWebVitals()`
4. Настроить `nuxt.config.ts` секцию `hints.features`

---

### 5. ✅ Установить `@nuxt/a11y`

**Статус:** ожидает утверждения

**Что даёт:**
- Автоматическое тестирование доступности (a11y) в Nuxt DevTools
- Движок axe-core (тот же, что в Lighthouse) — проверка WCAG 2.0/2.1/2.2
- Подсветка проблемных элементов на странице с группировкой по severity
- Auto-Scan при навигации и взаимодействии
- **Alpha-версия**, но легко удаляется (dev-only, одна команда)

**Что нужно сделать:**
1. `npx nuxt module add a11y`
2. При необходимости настроить `nuxt.config.ts` секцию `a11y`

---

### 6. Создать отдельный MCP-сервер для документации Vue 3

**Статус:** ожидает утверждения

**Зачем:**
- Обеспечить AI-агенту доступ к актуальной документации Vue 3 (vuejs.org)
- Аналогично тому, как `strapi-mcp` даёт доступ к Strapi API

**Что нужно сделать:**
- Разработать MCP-сервер (через `@modelcontextprotocol/sdk`), который будет:
  - Принимать запросы по разделам документации
  - Извлекать контент с `https://vuejs.org`
  - Возвращать в структурированном виде (text/markdown)
- Добавить конфигурацию в `kilo.json` (аналогично strapi-mcp или docs-memory)
- Протестировать подключение и работу инструментов