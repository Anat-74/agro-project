# Plan refactoring

## Phase 1 (1-7): ChatAssistant
- dialog + UInput + ChatAssistantButton + clean UButton

## Phase 2 (8-13): DiscountProduct modal
- lazy fetch + modal + SEO

## Phase 3 (14-15): Global patterns
- useDebounce + localization via computed (17 components)

## Phase 4 (16-17): ShowHamburger optimization
- close() on all links + lazy loading + computed key + errors

## Phase 5 (18-19): Pages [lang]
- ProductDetailLayout.vue (eliminate 80% duplication)
- Computed keys for pagination (replace manual watch+refresh)
- cartshopping.vue: 8 fixes
  * name-class -> variant (bug fix)
  * createError -> return [] (no 404 on empty)
  * setTimeout cleanup in onUnmounted
  * aria-label via translations (not hardcoded)
  * NuxtImg -> UImage
  * useSeoMeta -> watchEffect
  * box-shadow -> CSS variables
  * > * { min-width: 0 } on grid containers
- Cleanup: catch{throw}, as any->types, useProductLink in ShowHamburger

Done: 5 phases, 20 sections

## Cart components analysis

### CartShopping.vue
- getProductLink() duplicates useProductLink composable -> replace
- NuxtImg with direct Strapi URL -> UImage

### OrderForm.vue
- alert('Error:') -> proper UI notification (AppNotification)
- raw input fields -> UInput (Phase 1 component)
- border: 1px -> toRem(1)
- box-shadow rgba -> CSS variables

### ProductStatus.vue
- No BEM: in-stock/to-order -> product_status_in-stock / product_status_to-order
- Inline type in defineProps -> interface Props
- Translations directly in template

---
All components covered. Plan complete.

## Phase 6: app.vue optimization

### Issues and fixes
- updateHead() called twice (line 145 + watch) -> single watch with immediate:true
- useHead() fully recreated on every change -> split static (fonts, preconnect) and dynamic (SEO, hreflang)
- import { useLocale } -> redundant (auto-imported by Nuxt)
- ./locales/appSeo -> ~/locales/appSeo (inconsistent path)
- ["ru","be"] hardcoded -> extract to constant or use LocaleCode type
- updateHead() as regular function -> rewrite as watchEffect

### Kept as-is
- PWA components commented (dev mode)
- provide/inject for isContacts (legitimate usage in root component)

---
## Final execution sequence
Phase 1: ChatAssistant - dialog + UInput + ChatAssistantButton + clean UButton
Phase 2: DiscountProduct modal - lazy fetch + SEO
Phase 3: Global patterns - useDebounce + localization computed
Phase 4: ShowHamburger optimization
Phase 5: Pages [lang] - ProductDetailLayout + cart components + app.vue

### Phase 5 expansion (cart components + app.vue)
5a. CartShopping.vue - useProductLink + UImage
5b. OrderForm.vue - UInput + AppNotification + border toRem + box-shadow vars
5c. ProductStatus.vue - BEM fix + interface Props
5d. app.vue - watchEffect + split useHead + clean imports

## Phase 7: Stabilization (after refactoring, before deploy)

### 7a. Core Web Vitals — custom composable
Instead of @nuxtjs/web-vitals module:
- npm i web-vitals (Google lib, 4KB, required for browser metrics)
- Create composables/useWebVitals.ts
- Track LCP, CLS, INP, FCP, TTFB on each page
- Send to: custom API endpoint + console log (dev) + optionally GA/GTM

### 7b. Tests with @nuxt/test-utils
- npm i -D @nuxt/test-utils vitest @vue/test-utils happy-dom
- Create vitest.config.ts with Nuxt environment
- Write tests for critical components:
  * Dialog open/close (ShowHamburger, DiscountProduct)
  * Localization switching (computed pattern)
  * Navigation links (useProductLink, NuxtLink paths)
  * Cart actions (add/remove/quantity)
  * SEO meta tags (useSeoMeta correctness)

### 7c. E2E smoke tests (optional)
- Install playwright-core
- Critical user flows: homepage -> category -> product -> cart
- Dialog opening and data loading
- Language switching

---
## Full execution sequence
Phase 1: ChatAssistant refactoring
Phase 2: DiscountProduct modal
Phase 3: Global patterns (useDebounce, localization)
Phase 4: ShowHamburger optimization
Phase 5: Pages [lang] + cart components + app.vue
Phase 6: app.vue optimization
Phase 7: Stabilization (Web Vitals + Tests) -> before deploy
