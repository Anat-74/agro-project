# Refactoring Plan — Agro Market

## Правила работы
- После каждого шага — краткий отчёт о сделанном.
- Переход к следующему шагу ТОЛЬКО после твоего одобрения.
- Шаги выполняются строго последовательно, один за другим.
- Ты проверяешь что всё работает, ничего не сломалось, и только потом говоришь «дальше».
 - Комиты строго после одобрения пользователя

## Контекст проекта

### Tech Stack
- Nuxt 4.4.8, Nitro 2.13.2, Vite 7.3.5, Vue 3.5.38
- SCSS (BEM), TypeScript, Pinia, Strapi 5.48.1, Netlify SSR

### Strapi v5 — критичные правила

1. **locale** — всегда внутри `filters` с `$eq`
2. **deep filters** — только вложенный синтаксис (dot notation `"category.slug"` → 400 ошибка с `@nuxtjs/strapi`)
3. **populate** для компонентов — без поля `id`
4. **sort** — обязательно массивом: `sort: [value]`
5. **`as any`** — обязательно для всех `find()` с generic-типами
6. **`getProductLink()`** — требует populated `category`/`subcategory`
7. **`fields`** — избегать `id` для media и components

### Single Media для изображений категорий/подкатегорий
- Поле `image`: теперь single media (не массив). Тип: `Image[]` → `Image | null`
- Доступ: `item.image?.url` (не `item.image[0]?.url`)
- Продукты: `image` остаётся массивом, `mainImage` — single media

### Соглашение об именовании dialog-компонентов
- **Панель** (без backdrop, `useShowMethod: true`): префикс `Show`. Пример: `ShowHamburger.vue`
- **Модальное окно** (с backdrop, `useShowMethod: false`): префикс `ShowModal`. Пример: `ShowModalDiscountProduct.vue`
- **Чистая модалка** без кнопки открытия: суффикс `Modal`. Пример: `ConfirmDeleteModal.vue`

---

## Web Vitals (мониторинг производительности)

### Структура
- `composables/useWebVitals.ts` — сбор LCP, CLS, INP
- `server/api/web-vitals.post.ts` — приём метрик
- `server/api/web-vitals.get.ts` — просмотр (GET /api/web-vitals)
- `server/data/web-vitals.json` — файл с данными

### Как проверить в новой сессии
1. Запустить `npm run dev` (frontend)
2. Походить по страницам сайта
3. Открыть `http://localhost:3000/api/web-vitals` — JSON со всеми метриками
4. Или прочитать файл: `cat frontend/server/data/web-vitals.json`

### Известные проблемы
- **CLS** — сбрасывается при смене pathname (накопление между страницами пофикшено)
- **LCP** — холодный старт после сборки может выдавать 37-38s (единоразово)

---

## Тесты (unit-тестирование)

### Команды
- `npx vitest run` — полный прогон
- `npx vitest run --reporter=verbose` — подробный вывод

### Написанные тесты
| Тест | Что проверяет | Статус |
|------|---------------|--------|
| `useDialog` | open → isOpen = true, close → isOpen = false, reuse по ID | ✅ 2 теста |
| `useDebounce` | возвращает функцию, есть cancel | ✅ 2 теста |

### Что НЕ протестировано (и почему)
- **Компоненты** (UButton, UInput) — блокируются `@nuxtjs/color-mode`
- **Сторы** (useCartStore) — блокируются Nuxt auto-imports (`#imports`)

---

# Личный кабинет (User Profile) — план реализации

Strapi Users & Permissions + JWT.

### API эндпоинты (встроенные в Strapi)
| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/auth/local/register` | Регистрация |
| POST | `/api/auth/local` | Вход |
| GET | `/api/users/me` | Текущий пользователь |
| POST | `/api/auth/forgot-password` | Сброс пароля |
| POST | `/api/auth/reset-password` | Новый пароль |

### Бэкенд (Strapi)
- **A1.** Права доступа: Public → auth, Authenticated → order по email
- **A2.** Связь Order → User (по email или relation)
- **A3.** Email-провайдер

### Фронтенд (Nuxt)
- **B1.** `stores/useAuthStore.ts` — user, token, login/register/logout
- **B2.** `composables/useAuth.ts` — обёртка с isAuthenticated
- **B3.** `middleware/auth.ts` — редирект если нет токена
- **B4.** Страницы: login / register / cabinet / cabinet/[orderId]
- **B5.** Компоненты: AuthLogin, AuthRegister, OrderHistory
- **B6.** Локали: `locales/auth.ts` + `locales/cabinet.ts`

**Оценка времени:** ~2.5 часа

---

## Vue Vapor Mode — ожидается

Vapor Mode пока **не вышел** — последняя версия Vue **3.5.38**. Информация о релизе 3.6 с Vapor оказалась преждевременной.

Когда Vapor появится:
1. `script setup vapor` — opt-in на уровне компонента
2. Приоритет: компоненты с `v-for` (ProductCard, DiscountProduct, Pagination)
3. Производительность: до 97% быстрее рендеринг, -50% памяти

---

## Предложения по развитию

| Задача | Описание | Когда актуально |
|--------|----------|----------------|
| E2E тесты (Playwright) | Полная проверка корзины и заказа | Перед продакшеном |
| Страница /search | Отдельная страница поиска | После личного кабинета |
| Оптимизация изображений | Responsive image sets, srcset | При высоком CLS |
| i18n SEO | hreflang, sitemap для be | При запуске белорусской версии |
| PWA + Service Worker | Офлайн-режим | После стабилизации |
| Анимации переходов | Page transitions, skeleton screens | После вёрстки |
| Storybook | Каталог компонентов | При росте количества компонентов |

---

## Тестирование с телефона (локальная сеть)

### Твой локальный IP
```
192.168.1.7
```

### Запуск
1. **Фронтенд (Nuxt):** `cd frontend && npm run dev -- --host`
2. **Strapi:** `cd backend && npm run dev`

### Доступ с телефона
| Сервис | URL |
|--------|-----|
| Frontend | `http://192.168.1.7:3000/ru` |
| Strapi Admin | `http://192.168.1.7:1337/admin` |

### `.env` (frontend)
Поменять `NUXT_PUBLIC_STRAPI_URL` на локальный IP:
```
NUXT_PUBLIC_STRAPI_URL=http://192.168.1.7:1337
```

### Права в Strapi для Authenticated
После логина Strapi переключает на роль **Authenticated** — нужно добавить те же права что у Public:
- `product` → find, findOne
- `category` → find, findOne
- `subcategory` → find, findOne
- `global` → find
- `home-page` → find
- `order` → find, findOne (уже включено)


---

## Личный кабинет — файловая структура

### Сейчас
```
components/
├── AuthLogin.vue
├── AuthRegister.vue
├── OrderHistory.vue

pages/[lang]/
├── login.vue
├── register.vue
├── cabinet/index.vue
├── cabinet/[orderId].vue

locales/
├── auth.ts
├── cabinet.ts

stores/
├── useAuthStore.ts
```

### Цель (рефакторинг)
```
components/auth/
├── AuthLogin.vue
├── AuthRegister.vue
├── OrderHistory.vue

pages/[lang]/auth/
├── login.vue
├── register.vue

pages/[lang]/cabinet/
├── index.vue
├── [orderId].vue

locales/
├── auth.ts
├── cabinet.ts

stores/
├── useAuthStore.ts        ← одна стора, папка избыточна
```


---

## Технический долг — useAsyncData (Nuxt 4)

### Проблема
В `OrderHistory.vue` и `cabinet/[orderId].vue` ключ `useAsyncData` — **plain string**, а не computed. При динамической навигации между заказами кэш не сбрасывается → показывает старые данные.

### Что нужно сделать
1. Заменить ключи на **computed** или функцию-геттер:
   ```ts
   const orderKey = computed(() => `order-${route.params.orderId}`)
   useAsyncData(orderKey, async () => { ... })
   ```
2. Заменить `pending` на **`status`** (новый синтаксис Nuxt 4):
   ```ts
   // Было
   const { data, pending } = useAsyncData(...)
   if (pending.value)

   // Стало
   const { data, status } = useAsyncData(...)
   if (status.value === 'pending')
   // status: 'idle' | 'pending' | 'success' | 'error'
   ```
3. Добавить **`transform`** для нормализации ответов Strapi v5:
   ```ts
   transform: (response) => response.data
   ```
4. Рассмотреть **`getCachedData`** для кэширования заказов (необязательно)

### Приоритет
Средний — баг с кэшированием заказов при динамической навигации.


---

## Личный кабинет — следующие шаги

### 1. Восстановление пароля (приоритет: высокий)
- **B7.** Страницы: `/forgot-password`, `/reset-password`
- **B8.** Компоненты: `AuthForgotPassword.vue`, `AuthResetPassword.vue`
- **B9.** Локали: forgot/reset messages
- **Бэкенд:** Email-провайдер (A3) — nodemailer или sendmail
- Strapi уже предоставляет эндпоинты:
  - POST `/api/auth/forgot-password` — запрос сброса
  - POST `/api/auth/reset-password` — новый пароль
- `useStrapiAuth().forgotPassword(data)` и `useStrapiAuth().resetPassword(data)` уже готовы

### 2. Привязка заказов к user при оформлении (приоритет: средний)
- В `useOrderStore.ts` при создании заказа передавать `user: authStore.user.id` (если авторизован)
- Заказы не потеряются даже при смене email
- Поле `user` в Order уже добавлено в Strapi

### 3. Статусы заказов с цветными badge (приоритет: средний)
- Стилизация статусов в `OrderHistory.vue` и `cabinet/[orderId].vue`
- 🟢 Новый, 🟡 В обработке, 🔵 Подтверждён, ✅ Доставлен, 🔴 Отменён

### 4. Редактирование профиля (приоритет: низкий)
- Страница `/cabinet/edit`
- Смена имени, email, пароля
- `useStrapi().update('users/me', data)`

### 5. Аватар пользователя (приоритет: низкий)
- Загрузка фото через Strapi Upload
- Отображение в `/cabinet` и в хедере

---

## Улучшение пользовательского опыта (UX)

### Навигация и UI
- [ ] **Иконка пользователя в хедере** — показывать аватар/инициалы вместо иконки, когда авторизован
- [ ] **Активный пункт меню** — подсвечивать «Личный кабинет» в навигации когда внутри
- [ ] **Мобильное меню** — добавить ссылку на кабинет в ShowHamburger

### Обратная связь
- [ ] **Тосты/уведомления** при входе, выходе, регистрации, смене пароля (через `useNotification`)
- [ ] **Skeleton загрузки** — вместо «Загрузка...» показывать скелетон-карточки в OrderHistory
- [ ] **Плавные переходы** между страницами кабинета
- [ ] **Подтверждение действия** — «Вы уверены, что хотите выйти?» (ConfirmDeleteModal)

### Формы
- [ ] **Валидация полей** прямо в форме (email, длинна пароля) до отправки
- [ ] **Индикатор сложности пароля** при регистрации
- [ ] **Автофокус** на первом поле формы
- [ ] **Сохранять email** между сессиями (LocalStorage) — не вводить заново при неудачном входе

### Заказы
- [ ] **Фильтры в истории** — по статусу, диапазону дат
- [ ] **Пагинация** — если заказов больше 50 (сейчас limit 50)
- [ ] **Повтор заказа** — кнопка «Заказать ещё раз» добавляет те же товары в корзину
- [ ] **Email-уведомление** при смене статуса заказа


### 6. Улучшение ошибок в формах (приоритет: высокий)
**Проблема:** Пользователь не понимает что сделал не при входе/регистрации — ошибка общая («Invalid identifier or password») или не отображается совсем.

**Что сделать:**
- [ ] **Client-side валидация** перед отправкой:
  - Email — проверка формата (`/^\S+@\S+\.\S+$/`)
  - Пароль — минимум 6 символов
  - Пустые поля — подсветка + сообщение
- [ ] **Маппинг ошибок Strapi** в понятные сообщения через локали (`auth.ts`):
  - `"Invalid identifier or password"` → «Неверный email или пароль»
  - `"Email already taken"` → «Этот email уже зарегистрирован»
  - `"Username already taken"` → «Это имя пользователя уже занято»
  - `"password must be at least 6 characters"` → «Пароль должен содержать минимум 6 символов»
- [ ] **Field-level ошибки** — подсветка красным конкретного поля, а не только общая надпись


---

## Перенос файлов в каталоги (рефакторинг структуры)

### Задача
Переместить файлы личного кабинета в отдельные подкаталоги для порядкa.

### Что сделать
1. **`components/` → `components/auth/`:**
   - `AuthLogin.vue`
   - `AuthRegister.vue`
   - `OrderHistory.vue`

2. **`pages/[lang]/` → `pages/[lang]/auth/`:**
   - `login.vue`
   - `register.vue`

3. **Обновить импорты** в файлах, которые ссылаются на старые пути:
   - `pages/[lang]/login.vue` — импорт `AuthLogin`
   - `pages/[lang]/register.vue` — импорт `AuthRegister`
   - `pages/[lang]/cabinet/index.vue` — импорт `OrderHistory`

Nuxt auto-imports компоненты по имени, так что если компоненты останутся с теми же именами — импорты обновлять не нужно (кроме страниц).

### Приоритет
Низкий — структурный, функциональность не меняет.


---

## Выполнено без проверки (18.06.2026)

### Перенос компонентов
- `components/AuthLogin.vue` → `components/auth/AuthLogin.vue`
- `components/AuthRegister.vue` → `components/auth/AuthRegister.vue`
- `components/OrderHistory.vue` → `components/auth/OrderHistory.vue`

### `useAsyncData` — обновление до Nuxt 4 API
- **`OrderHistory.vue`:** ключ заменён на `computed` (`ordersKey`), `pending` → `status`, добавлен `transform: (r) => r?.data`
- **`cabinet/[orderId].vue`:** ключ заменён на `computed` (`orderKey`), `pending` → `status`, добавлен `transform: (r) => r?.data`

### Улучшение ошибок в формах
- **`useAuthStore.ts`:** маппинг ошибок Strapi на локали (`authTranslations`), добавлен `fieldErrors` для подсветки конкретного поля
- **`AuthLogin.vue`:** client-side валидация (email, обязательные поля), проброс `fieldErrors`, отключение кнопки пока форма невалидна
- **`AuthRegister.vue`:** client-side валидация (email, username 2+, пароль 6+), проброс `fieldErrors`
- **`login.vue` / `register.vue`:** передача `:field-errors="authStore.fieldErrors"` в компоненты

---

## Что осталось

### Высокий приоритет
- [ ] **Восстановление пароля** (страницы `/forgot-password`, `/reset-password`, компоненты, локали)
- [ ] **Настроить Email-провайдер** в Strapi (A3)


### Средний приоритет
- [x] **Привязка заказов к user** — `useOrderStore.ts` передаёт `user.documentId` при создании, если авторизован (18.06.2026)
- [x] **Статусы заказов** — цветные badge 🟢 🟡 🔵 ✅ 🔴 в OrderHistory и деталях заказа (18.06.2026)
- [x] **Тосты/уведомления** — при входе (`login.vue`), регистрации (`register.vue`), выходе (`cabinet/index.vue`) (18.06.2026)
- [x] **Skeleton загрузки** — `OrderHistorySkeleton.vue` с CSS shimmer-анимацией, подключен в OrderHistory (18.06.2026)

### Низкий приоритет
- [ ] **Перенос страниц login/register** в `pages/[lang]/auth/` (изменит URL)
- [x] **Редактирование профиля** — страница `/cabinet/edit` с формой (имя, email, смена пароля), ссылка из кабинета (18.06.2026)
- [ ] **Аватар пользователя** — загрузка фото через Strapi
- [x] **Иконка пользователя с инициалами** в хедере — `header__initials` с первой буквой имени, `cil:user` для гостей (18.06.2026) (вместо `cil:user`)
- [ ] **Фильтры заказов** по статусу
- [ ] **Пагинация заказов** (сейчас limit 50)
- [ ] **Повтор заказа**
- [ ] **Индикатор сложности пароля**
- [x] **ConfirmDeleteModal при выходе** — компонент `components/auth/ConfirmDeleteModal.vue`, подключен к кнопке «Выйти» в `/cabinet` (18.06.2026)

---

## Чеклист проверки (выполнить на ПК)

### 1. Проверка Strapi
- [ ] Запустить `cd backend && npm run dev` — Strapi стартует без ошибок
- [ ] Открыть `http://localhost:1337/admin` — админка работает
- [ ] **Settings → Users & Permissions → Roles → Public**: включены `auth.register`, `auth.login`
- [ ] **Settings → Users & Permissions → Roles → Authenticated**: включены `order.find`, `order.findOne`, `product.find`, `product.findOne`, `category.find`, `category.findOne`, `subcategory.find`, `subcategory.findOne`, `global.find`, `home-page.find`

### 2. Проверка фронтенда
- [ ] Запустить `cd frontend && npm run dev`
- [ ] Открыть `http://localhost:3000/ru` — главная загружается без ошибок в консоли
- [ ] Нажать **иконку пользователя** (рядом с корзиной) → переход на `/ru/login`

### 3. Регистрация
- [ ] Открыть `/ru/register`
- [ ] Ввести имя, email, пароль (мин. 6 символов)
- [ ] Нажать «Зарегистрироваться» — появляется toast «Аккаунт создан!» → редирект в кабинет
- [ ] В кабинете видны: аватар (первая буква имени), имя, email, «История заказов» (пусто)

### 4. Профиль — иконка в хедере
- [ ] В хедере вместо иконки `cil:user` — синий кружок с первой буквой имени
- [ ] Навести на кружок — scale 1.15

### 5. Редактирование профиля
- [ ] В кабинете нажать «Редактировать профиль» → страница `/cabinet/edit`
- [ ] Поменять имя → «Сохранить» → toast «Профиль сохранён»
- [ ] Вернуться в кабинет — имя обновлено
- [ ] Сменить пароль: заполнить текущий + новый + подтвердить → сохранить

### 6. Выход из аккаунта
- [ ] В кабинете нажать «Выйти» → появляется модалка «Вы уверены?»
- [ ] Нажать «Отмена» → ничего не происходит
- [ ] Нажать «Выйти» → toast «Вы вышли из аккаунта» → редирект на `/ru/login`
- [ ] После выхода иконка в хедере — `cil:user` (без кружка)

### 7. Вход
- [ ] На `/ru/login` ввести email/пароль
- [ ] Нажать «Войти» → toast «Вы вошли как ...» → редирект в кабинет

### 8. Ошибки форм
- [ ] На `/ru/register` оставить поля пустыми → кнопка заблокирована
- [ ] Ввести `test` в email → подсветка красным «Введите корректный email»
- [ ] Ввести `12` в пароль → подсветка красным «Минимум 6 символов»
- [ ] На `/ru/login` ввести неверный пароль → красная надпись «Неверный email или пароль»

### 9. Заказы
- [ ] Сделать тестовый заказ через корзину
- [ ] Зайти в кабинет — заказ отображается в списке с цветным badge (🟢 Новый)
- [ ] Нажать «Просмотр» → страница деталей заказа со статусом-badge
- [ ] Нажать «← Назад к заказам» → вернуться в список

### 10. Skeleton
- [ ] Если заказов нет — виден текст «У вас пока нет заказов»
- [ ] Если заказы грузятся — видна CSS shimmer-анимация (3 серых карточки)

### 11. Редирект при отсутствии токена
- [ ] Открыть `/ru/cabinet` в приватном окне (без сессии) → редирект на `/ru/login?redirect=%2Fru%2Fcabinet`
- [ ] После входа — возврат в кабинет

### 12. Навигация
- [ ] Переходить между страницами: главная, категории, товары, корзина — всё загружается (проверить что нет 403 из-за прав Authenticated)
- [ ] Переключить язык (ru/be) — страницы логина/регистрации/кабинета работают

